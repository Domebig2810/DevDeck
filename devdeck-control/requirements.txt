# Plattformübergreifende Abhängigkeiten (macOS + Windows).
# pywebview zieht seine plattformspezifischen Backends selbst:
#   macOS:   pyobjc-* (WKWebView)
#   Windows: pythonnet (Edge WebView2)
pillow==11.3.0
pyserial==3.5
pywebview==6.2.1
typing_extensions==4.15.0

# Nur für die alte PyQt-Oberfläche (ui/, nicht mehr vom Hauptprogramm genutzt):
# PyQt6==6.10.2
# pyqtdarktheme==2.1.0
